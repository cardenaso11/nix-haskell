/** @externs @suppress {duplicate} */
// A name the fixture claims the runtime defines elsewhere, so closure-compiler
// would leave it alone. Nothing here is ever compiled: the checks only carry the
// file through the option that names it.

/** @type {*} */
var everyOption;
